name: security
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1"
jobs:
  scan:
    uses: cognitum-one/.github/.github/workflows/security-scan.yml@8f1f1b465553d3838f5e592cf951cb0f9a2dd7b5
