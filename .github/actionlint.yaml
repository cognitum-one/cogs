# Custom self-hosted runner labels, so actionlint does not reject them.
#
# `gcp-bypass` is the fleet stood up for the GitHub-hosted-minutes billing
# stop: gha-runner-bypass in us-central1-a, four runner processes, NO service
# account. Matches cognitum-one/website and cognitum-one/management.
self-hosted-runner:
  labels:
    - gcp-bypass
