[cog]
id = "fixture-secret"
version = "1.0.0"

[integrations.tailscale]
enabled = true
tailnet = "example.ts.net"
hostname = "fixture-secret"
tags = ["tag:cog"]
ephemeral = true
key_expiry_seconds = 3600
credential_binding = "tailscale-oauth-client"
secret_manager_ref = "projects/example/secrets/tailscale-oauth-client/versions/1"
